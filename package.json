{
  "name": "nfd",
  "version": "1.0.0",
  "description": "Telegram private-message forwarding bot running on Cloudflare Workers",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
